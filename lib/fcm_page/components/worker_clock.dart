import 'package:flutter/material.dart';

class WorkerClock extends StatelessWidget {
  const WorkerClock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "IMPLEMENT THE TIME",
      style: TextStyle(
        backgroundColor: Colors.red
      ),
    );
  }
}
